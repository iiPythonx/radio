[project]
name = "webradio"
version = "0.1.0"
description = "A next generation web radio."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.115.8",
    "pydub>=0.25.1",
    "uvicorn[standard]>=0.34.0",
]
